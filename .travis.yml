matrix:
  include:
    - language: node_js
      cache: yarn
      script:
        - yarn lint
    - language: python
      dist: xenial
      python: 3.7
      install:
        - pip install -r requirements.txt -e .
      script: pylint flocx_ui/**/*.py